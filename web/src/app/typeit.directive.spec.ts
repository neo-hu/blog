import { TypeitDirective } from './typeit.directive';

describe('TypeitDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeitDirective();
    expect(directive).toBeTruthy();
  });
});
